import React from 'react';
import ReactDOM from 'react-dom';

import App from './App'

ReactDOM.render(<App />, document.getElementById('root'));


// function ReactDom.render(thingToRender, where) {
//     let elem = thingToRender.render()
//     where.appendChild(elem);
// }